# llamajet-driver-ts

`llamajet-driver-ts` is a TypeScript driver for the Teknic ClearCore motion controller, built for the Llamajet project. It provides an interface for managing serial communication, enabling precise control over motors and I/O pins.

